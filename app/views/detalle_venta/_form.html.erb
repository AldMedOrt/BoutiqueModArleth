<%= form_with(model: detalle_ventum) do |form| %>
  <% if detalle_ventum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(detalle_ventum.errors.count, "error") %> prohibited this detalle_ventum from being saved:</h2>

      <ul>
        <% detalle_ventum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :precio_total, style: "display: block" %>
    <%= form.text_field :precio_total %>
  </div>

  <div>
    <%= form.label :cantidad, style: "display: block" %>
    <%= form.number_field :cantidad %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
